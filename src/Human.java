abstract class Human extends Agent {
    protected int age;
    public Human(int x, int y){
        super(x,y);
    }

    @Override
    public void go(Simulation map) {
        super.go(map);
    }
}
